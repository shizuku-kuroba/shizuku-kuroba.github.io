# Pluvia 個人網站維護指南

這份指南是給未來的自己用的：不必修改網站程式，也能新增或編輯文章、替換圖片、更新首頁與作品頁內容，並把完成的內容推到 GitHub。

專案資料夾：`D:\1. Project\P13 個人網站\astro-theme-pure`

## 先記住這四件事

1. 文章放在 `src/content/blog/`；一篇文章就是一個資料夾。
2. 圖片最好和文章的 `index.md` 放在同一個資料夾，路徑最不容易出錯。
3. 文章先以 `draft: true` 儲存；要公開時才改成 `draft: false`。
4. 每次更新前先檢查、再推送；目前請只推到 `astro-site`，不要碰舊站的 `master`。

---

## 目錄地圖

| 想修改的內容 | 檔案或資料夾 |
| --- | --- |
| 新增、編輯文章 | `src/content/blog/<文章網址名稱>/index.md` |
| 文章封面與內文圖片 | 與該篇 `index.md` 同一資料夾 |
| 首頁自介、經歷、Elements、底部語錄 | `src/pages/index.astro` |
| About 頁 | `src/pages/about/index.astro` |
| Projects 頁、GitHub Activities 使用者名稱 | `src/pages/projects/index.astro` |
| PICT／Tumblr 圖庫 | `src/pages/pict/index.astro` |
| PICT 圖片資料處理 | `src/utils/tumblr.ts` |
| 導覽列、網站名稱、描述、分享按鈕、留言開關 | `src/site.config.ts` |
| 一般顏色與樣式 | `src/assets/styles/app.css` |
| 文章頁頁首、目錄、文章底部按鈕 | `packages/pure/components/pages/` |

一般內容更新時，優先改 `src/` 裡的檔案；`packages/pure/` 比較像網站的共用骨架，除非是所有文章頁都要一起改，否則不必碰它。

---

## 新增一篇文章

### 1. 建立文章資料夾

在 `src/content/blog/` 新增一個資料夾。名稱會成為網址的一部分，請用英文小寫、數字與連字號，例如：

```text
src/content/blog/my-first-post/
```

完成後，文章網址會是：

```text
/blog/my-first-post
```

不要用空白、中文、底線或特殊符號作為資料夾名稱；標題則可以正常使用中文。

### 2. 建立 `index.md`

在新資料夾內建立 `index.md`，複製下面範本後再改內容。

```md
---
title: '文章標題，請控制在 60 個字元內'
description: '這段會顯示在文章列表與搜尋結果，請控制在 160 個字元內。'
publishDate: '2026-09-20'
updatedDate: '2026-09-20'
heroImage:
  src: './cover.jpg'
  alt: '封面圖片的文字說明'
  color: '#659EB9'
tags:
  - 黑執事
  - 文化觀察
language: 'Chinese'
draft: true
comment: true
---

這裡開始寫文章。
```

必填欄位是 `title`、`description`、`publishDate`。其餘欄位可依需要保留或刪除。

欄位說明：

| 欄位 | 用途 |
| --- | --- |
| `title` | 文章名稱；最多 60 個字元。 |
| `description` | 文章摘要；最多 160 個字元。 |
| `publishDate` | 發布日期，格式固定為 `YYYY-MM-DD`。 |
| `updatedDate` | 修改日期；有更新才填。 |
| `heroImage` | 文章封面。`src` 是圖片檔名；`alt` 是圖片替代說明。 |
| `color` | 封面讀取前顯示的底色；可沿用 `#659EB9`。 |
| `tags` | 分類標籤；英文會自動轉小寫並去除重複。 |
| `language` | 可寫 `Chinese`、`Japanese`、`English` 等。 |
| `draft` | `true` 是下架／草稿，`false` 才會公開出現在文章列表。 |
| `comment` | 這篇文章是否允許留言；目前全站留言系統尚未啟用，設為 `true` 也不會顯示留言區。 |

### 3. 放入封面圖片

把封面檔案放在文章資料夾中，例如：

```text
src/content/blog/my-first-post/cover.jpg
```

並確認前置資料中的路徑完全一致：

```yaml
heroImage:
  src: './cover.jpg'
```

圖片建議：

- 檔名用英文、數字與連字號，例如 `cover.jpg`、`character-study-01.png`。
- 優先使用 `.jpg`、`.png` 或 `.webp`。
- 封面是直式圖時，文章頁會自動裁成固定高度的橫幅；重要人物或文字請盡量放在「上方中央」附近。
- 文章內圖片也建議放同一資料夾，之後搬移或備份不容易漏檔。

### 4. 撰寫內文

內文使用 Markdown。文章標題已經在前置資料的 `title` 填過，因此正文請從二級標題 `##` 開始。

```md
## 第一節標題

這是一段文字。段落之間請空一行。

### 小標題

- 清單項目一
- 清單項目二

1. 有順序的項目
2. 第二項

![這張圖的說明](./detail.jpg)

| 名稱 | 說明 |
| --- | --- |
| A | 內容 A |
| B | 內容 B |
```

常用格式：

```md
**粗體**
*斜體*
[連結文字](https://example.com)
> 引言或原文引用
`程式或術語`
```

### 5. 圖片來源與參考資料

圖片來源請放在圖片下方或文章末尾，採用一般段落即可，不要用 `>` 引言框，視覺會更自然。

```md
*封面圖片來源：[Yen Press](https://example.com)*
```

參考資料每一筆之間空一行，Markdown 才會正確換行：

```md
## 參考資料

1. 作者，《書名》，出版社，年份。

2. [網站或文章名稱](https://example.com)，查閱日期：2026-09-20。
```

### 6. 發布或下架

寫作期間：

```yaml
draft: true
```

要公開：

```yaml
draft: false
```

要暫時下架而保留文章、圖片與網址，就改回 `draft: true`。這不會刪除檔案。

---

## 編輯既有文章

1. 到 `src/content/blog/` 找到該篇文章的資料夾。
2. 開啟裡面的 `index.md` 修改。
3. 有實質內容更新時，更新 `updatedDate`。
4. 改完後先在本機預覽並檢查，再推送 GitHub。

例如目前的〈瓷婚〉文章在：

```text
src/content/blog/porcelain-wedding-black-butler-229/index.md
```

如果遇到 `ImageNotFound`，幾乎都是這兩種原因：

- 圖片檔不在文章資料夾，或檔名與 `src: './檔名.jpg'` 不完全一致。
- 大小寫、副檔名或空白不同，例如實際檔案是 `Cover.JPG`，但文章寫成 `cover.jpg`。

---

## 更新網站頁面

### 首頁

開啟 `src/pages/index.astro`。這裡可以直接修改：

- 首頁自我介紹文字。
- 經歷時間線。
- Elements 的四張圖片與標題。
- Skills 清單。
- 頁面最下方的法文語錄。

Elements 現在是純展示卡片，沒有連結；若要新增連結，需在對應 `ProjectCard` 加上 `href='https://...'`。若不希望跳轉，維持現在的 `as='article'` 即可。

### About 頁

開啟 `src/pages/about/index.astro`。可修改自介、興趣、喜歡的作品與社群帳號。圖片通常要先放在 `src/assets/`，再在檔案最上方 `import` 後使用。

### Projects 頁與 GitHub Activities

開啟 `src/pages/projects/index.astro`。

- 專案卡內容在 `projects={[ ... ]}` 裡，每個專案有 `name`、`description` 與 `links`。
- GitHub 活動卡目前使用 `GitHubActivity username='shizuku-kuroba'`；日後換帳號時只改這個使用者名稱。
- 活動清單只會顯示 GitHub 的公開活動；私人 repo 或私人活動不會出現。

### PICT／Tumblr 圖庫

開啟 `src/pages/pict/index.astro`。

目前網站會讀取 `pluvia-sk.tumblr.com` 的公開貼文。每一篇貼文有多張圖片時，點進單篇頁面會取得該篇的全部圖片；列表只用第一張作預覽。

若圖庫抓不到資料，先確認：

1. Tumblr 帳號與該篇貼文都是公開狀態。
2. 圖片實際上傳到貼文中，不只是外部連結。
3. 圖片有成功發布到 Tumblr，再重新整理網站。

不要隨意改 `src/utils/tumblr.ts`，它負責處理一篇多圖貼文與各圖片尺寸。

### 網站名稱、導覽列、分享與留言

開啟 `src/site.config.ts`。

常見調整位置：

- `title`：網站名稱。
- `author`、`description`：作者與網站描述。
- `header.menu`：上方導覽列。
- `footer.social`：頁尾社群連結。
- `content.share`：文章底部的分享按鈕。現有可用值是 `weibo`、`x`、`bluesky`、`plurk`、`threads`。
- `waline.enable`：全站留言系統開關。

留言目前是關閉的：

```ts
waline: {
  enable: false
}
```

要真的讓訪客留言，不能只改成 `true`；還需要先建立並設定 Waline 留言服務的伺服器網址與資料庫。尚未完成這件事前，請維持 `false`，避免出現壞掉的留言區。

---

## 在本機預覽與檢查

第一次在新電腦使用時，先安裝 [Bun](https://bun.sh/)，並在專案資料夾開啟 PowerShell。

首次安裝套件：

```powershell
bun install
```

啟動本機網站：

```powershell
bun run dev
```

終端機會顯示一個本機網址，通常是 `http://localhost:4321`。用瀏覽器打開它；修改檔案後，頁面通常會自動更新。要停止伺服器時，在 PowerShell 按 `Ctrl + C`。

提交前至少跑一次檢查：

```powershell
bun run check
```

需要確認正式版可以建立時，再跑：

```powershell
bun run build
```

若檢查失敗，先讀最上面第一個錯誤；最常見的是 Markdown 前置資料的冒號、縮排、引號或圖片檔名打錯。

---

## 推送到 GitHub：安全流程

目前這個專案有兩個重要分支：

| 分支 | 用途 |
| --- | --- |
| `master` | 舊網站；請保留，不要直接覆寫。 |
| `astro-site` | 現在這份 Astro 個人網站的發布分支。 |

在專案資料夾開啟 PowerShell，依序執行：

```powershell
git status
```

確認畫面列出的修改就是你預期要上傳的檔案。接著只加入那些檔案，例如文章與圖片：

```powershell
git add src/content/blog/my-first-post/index.md src/content/blog/my-first-post/cover.jpg
```

建立一筆版本紀錄：

```powershell
git commit -m "publish: add my first post"
```

推送新網站分支：

```powershell
git push website HEAD:astro-site
```

這個指令會把目前的內容推到 GitHub 的 `astro-site`，不會改動舊站 `master`。完成後可在這裡查看檔案：

<https://github.com/shizuku-kuroba/shizuku-kuroba.github.io/tree/astro-site>

如果 `git status` 出現你不認識的檔案，先不要用 `git add .` 或 `git add -A`。請只加入你剛剛修改的那幾個檔案，避免把草稿、測試檔或私人圖片一起傳上去。

---

## 每次發文前的快速清單

- [ ] 標題與摘要已完成，且不超過字數限制。
- [ ] `publishDate` 格式正確。
- [ ] 封面檔案存在，`heroImage.src` 的名稱完全相同。
- [ ] 所有圖片都有說明文字與適當圖源標示。
- [ ] 正文從 `##` 開始，引用與圖片來源沒有混在一起。
- [ ] 發布前把 `draft` 改為 `false`；下架則設為 `true`。
- [ ] 已在本機看過手機寬度與桌面寬度的版面。
- [ ] `bun run check` 沒有錯誤。
- [ ] `git status` 只顯示預期變更。
- [ ] 已用 `git push website HEAD:astro-site` 推送。

## 不確定時的保守做法

先執行：

```powershell
git status
git diff
```

前者告訴你哪些檔案改了，後者讓你逐行確認改了什麼。看不懂或覺得不像自己要的內容時，先不要推送。保留一個清楚的小步驟，比一次塞進很多改動更容易回復與排錯。
